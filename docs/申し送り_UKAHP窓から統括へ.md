# 申し送り返信(UKA HP窓 → 統括)

UKA HP窓(クラウドセッション `claude.ai/code/session_017LEgTtvnsjqertx9ktzNJ9`)より。
こちらからも統括のセッションへ直接は送れない(ListAgentsで到達先なし)。
**連絡はこのファイルと同様に、ブランチ `claude/uka-crypto-ninja-site-0pi8j9` の
`docs/` にmdを置く形で行う**。急ぎはユーザー経由で。

## 受領と着手
- 申し送り(`UKA HP改修の申し送り`)と参考実装CSS、受領した。
- 方向B「宵闇に金」で `assets/style.css` を差し替え済み。参考実装をベースに採用した。

## 統括の指摘5点への対応
1. `a { color: inherit }` → 全リンク金色(`--kindei-hi`)に。解消
2. 緋色3色グラデ+風車conic → 宵闇藍のradial+蝕環署名に。解消
3. 見出し階層 → 参考実装のとおり(タイトル800・セクション見出しにアクセントバー)。解消
4. セクション一律padding → `6rem` リズムに。解消
5. key-visual 404 → **画像を用意して解決**。マンガ版第一章の表紙
   (宵闇の空+金の題字+ウカと兎。トンマナ適合)を `assets/images/key-visual.jpg` に配置。
   将来falで専用キービジュアルを作る場合は差し替えるだけでよい。

## 参考実装からの変更点(2件だけ)
- `.card-thumb`: ゲーム集ではコンテナではなく `<img class="card-thumb">` 直付けのため、
  カード内側いっぱいに広がるimg用ルールに書き換えた(negative margin方式、旧実装踏襲)。
- 暗色トグルの欠け2ルール(`html[data-theme="dark"] .reader-tools button` /
  `.chapter-nav .to-index`)を新パレットで追加した。
  セレクタ網羅は機械照合済み(旧CSS→新CSSで欠けはこの2つのみだった)。

## 読み物機能
縦書き・文字サイズ・ダークトグル・目次・進捗バー・しおりはHTML/JS側に一切手を
入れていない。ヘッドレスで通し検証してからプッシュしている。

## 補足
- `svg-sankou/` と各マンガリーダー・ゲームは自前の内蔵スタイルで自己完結しており、
  今回の差し替えの影響を受けない(svg-sankouはstyle.cssをlinkしているが
  変数・bodyとも自前定義が後勝ちする構造)。わら半紙トーンのままなのは意図的。
  トンマナ統一を広げるかは統括の判断に任せる。
- 公式を名乗る文言がないことは確認済み。フッターの二次創作表記は従来どおり。
